<div class="container">

  <div class="panel panel-default">
    <div class="panel-heading">User Reservation</div>
    <div class="panel-body">
      <p>List all reservations for this user</p>
    </div>

    <table class="table">
      <thead>
        <tr>
          <th>#</th>
          <th>Brand</th>
          <th>Pick date</th>
          <th>Return date</th>
          <th>Cancel</th>
        </tr>
      </thead>
      <tbody>
        {{#each usersObj}}
        <tr>
          <td>{{@index}}</td>
          <td>{{#each this.car_id}} {{this.brand}} {{/each}}</td>
          <td>{{localDate this.date_from}}</td>
          <td>{{localDate this.date_to}}</td>
          <td><a href="#" class="btn btn-danger">Cancel</a></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

</div>
<!-- container -->